# .env.example — 실제 키를 채운 뒤 .env 로 복사 (git에 절대 커밋하지 않는다)

# 공공데이터포털 API 키 (디코딩된 값으로 입력)
WEATHER_API_KEY=여기에_입력

# Hugging Face 토큰 (Llama gate 통과 후 발급)
HF_TOKEN=hf_xxxx

# 학습 실험 추적 (선택)
WANDB_API_KEY=
MLFLOW_TRACKING_URI=
